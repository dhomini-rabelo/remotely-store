import styled from 'styled-components'

/* eslint-disable */
export const Span = {
  main: styled.span`
    font-family: 'Inter', 'Roboto', Arial, sans-serif;
    font-weight: 400;
    line-height: 125%;

    display: flex;
    align-items: center;
    color: #e63946 !important;

    img {
      position: relative;
      bottom: 1px;
    }

    span {
      color: #e63946 !important;
      font-size: 12px;
      margin-left: 0.25rem;
    }
  `,
}